name: viswa first CI/CD
on:
   push
jobs:
  call-template-job:
    uses: ./.github/workflows/reuse-template.yml
  sample-job:
    runs-on: ubuntu-latest
    needs: call-template-job
    steps:
      - run: echo "${{ vars.print_test }}"
  

        
       
